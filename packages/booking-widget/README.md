# @med-site/booking-widget

Packaging stub for the booking widget (`/src/widget`). **Do not change widget logic here.**

Wave 1 will embed this package in `apps/web` via a client boundary on `/booking`.

# WAVE 2: Entities & Widgets (Автономные V2 виджеты)

На этом этапе мы берем жестко "прибитые" к странице секции и превращаем их в универсальные FSD-виджеты.

- [x] **Шаг 2.1: DoctorsWidget (V2)**
    * Создать папку (или обновить текущую) `src/widgets/doctors-widget/`.
    * Добавить интерфейс пропсов: `directionId?: string`, `limit?: number`.
    * Перенести логику получения данных (`useDoctorsRepository`) ВНУТРЬ виджета, с учетом фильтрации по `directionId`.
    * Суб-шаг: вынести карточку врача в `src/entities/doctor/ui/DoctorCard.tsx`.
- [x] **Шаг 2.2: PromotionsWidget (V2)**
    * Создать `src/widgets/promotions-widget/`.
    * Интерфейс пропсов: `directionId?: string`, `limit?: number`.
    * Инкапсулировать `useQuery` для акций (или использование стора).
    * Вынести `PromotionCard` в `entities/promotion/`.
- [x] **Шаг 2.3: DirectionsWidget (V2)**
    * По просьбе ("не заметил что на направлениях что-то происходит при изменении вида"): добавить четкую анимацию для карточек направлений при смене пресета/вида.
    * Создать `src/widgets/directions-widget/`.
    * Реализовать варианты с плавными Framer Motion переходами.
- [x] **Шаг 2.4:** Для всех добавленных V2-виджетов предусмотреть обертку `AnimatePresence` или аналогичный механизм, который будет элегантно реагировать на смену "вида" из панели управления, как просил пользователь ("чтобы мы как группами могли эту анимацию отображать когда пресет меняем так и отдельно").

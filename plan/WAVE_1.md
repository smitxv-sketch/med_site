# WAVE 1: Shared & Layout (Фундамент и сетка)

В этой волне мы закладываем идеальный фундамент, чтобы решить все проблемы с шириной и "поехавшей" вёрсткой, а также подготовить дизайн-систему.

- [x] **Шаг 1.1:** Создать универсальный `shared/ui/Container.tsx` — единый источник правды для ширины контента (заменит разрозненные `max-w-7xl`, `max-w-md` и т.д. в разных местах проекта).
- [x] **Шаг 1.2:** Имплементировать `Container` в `src/app/layouts/MainLayout.tsx`. Проверить, как он работает со скроллом и safe-areas.
- [x] **Шаг 1.3:** Отрефакторить `src/widgets/header/ui/Header.tsx`, привязав его ширину к нашему `Container` или к единым CSS-переменным, чтобы шапка точно соответствовала основному блоку.
- [x] **Шаг 1.4:** Отрефакторить `src/widgets/bottom-nav/ui/BottomNav.tsx` (и варианты), устранив баг с "оторванной шириной". Заодно починить/обновить анимации по просьбам пользователя:
    * Добавить фейд (прозрачность по краям, градиентную маску "уходящую").
    * Обновить анимацию при переключении вариантов нижнего меню (сделать более плавный переход без резкого появления 'матового' фона).
- [x] **Шаг 1.5:** Провести общий QA: проверить, что `Header`, `Main` и `BottomNav` сидят на одной идеальной оси.

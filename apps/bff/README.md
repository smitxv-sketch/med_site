# Express BFF (Wave 0 skeleton)

Future home for QMS/WP/DomainDB integrations migrated from `/server`.

## Env

Copy `.env.example` to `.env`.

## Run

```bash
npm run dev -w @med-site/bff
# or from repo root: npm run dev:bff
```

Health: `GET http://localhost:3001/health`

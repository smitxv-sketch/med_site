# Widget template — copy to `src/widgets/{name}-widget/`

See `docs/WIDGET_TEMPLATE.md` for the full checklist.

Files:
- `schema.ts` — WidgetDefinition
- `config/exampleWidgetTheme.ts` — layout + design token refs
- `ui/ExampleWidget.tsx` — component using `resolveWidgetVariant`

Do **not** register `_template` in widgetManifest until copied and renamed.

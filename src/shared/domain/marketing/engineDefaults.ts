/**
 * Дефолты marketing engine (SSOT для preset/store).
 * Визуальные токены — в designTokens.ts, контент — в Repository/JSON.
 */
export const ENGINE_WIDGET_DEFAULTS = {
  heroDesktopVariant: 'C',
  heroMobileVariant: 'F',
  bottomNavVariant: 'E',
} as const;

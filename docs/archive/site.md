Новая задача, мы постепенно переходим к создани сайта


Действуй как Senior React/Next.js Architect.

КОНТЕКСТ ПРОЕКТА:
Мы разрабатываем сайт медицинской клиники. В проекте УЖЕ ЕСТЬ готовый и работающий модуль онлайн-записи, который тянет данные из внешней МИС. 

АРХИТЕКТУРНОЕ ПРАВИЛО №1 (СТРОГАЯ ИЗОЛЯЦИЯ):
Относись к модулю онлайн-записи как к независимому микросервису (Black Box). 
1. Помести все файлы, относящиеся к текущей онлайн-записи, в отдельную директорию (например, `/src/components/BookingWidget` или аналогичную).
2. ТЕБЕ СТРОГО ЗАПРЕЩАЕТСЯ изменять, рефакторить или удалять любой код внутри этой директории в ходе дальнейшей работы, если я прямо не попрошу об этом.
3. Модуль записи должен просто импортироваться на другие страницы как готовый компонент `<BookingWidget />`.

ТЕКУЩАЯ ЗАДАЧА:
Создать Главный экран (Landing Page) сайта клиники, который станет точкой входа. 
1. Главный экран должен иметь современный, чистый медицинский дизайн (Hero-секция и назвние Клиника "Источник").
2. На главном экране должна быть заметная кнопка "Записаться на прием", которая открывает наш `<BookingWidget />` в модальном окне.
3. Ниже Hero-секции создай блок "Новости клиники".

ИНТЕГРАЦИЯ СО STRAPI:
Блок новостей должен динамически получать данные из нашего Headless CMS. 
Используй REST API: `http://109.205.212.64:1337/api/news?populate=*`
Напиши функцию для фетчинга этих данных (используй fetch или axios) и выведи их в виде карточек (Заголовок, текст, картинка, если есть). Предусмотри состояние загрузки (Loading...) и обработку ошибок на случай, если Strapi недоступен.


Я верно понимаю, что ты не умеешь переносить файлы и будешь вынужден переписывать чтобы разместить в структеруе проекта, то есть сначала тебе надо селать проект структуры
Как именно будет вылядеть структура файлов.
Если я верно понимаю то онлайн запись это микросервис и у нас уже все готово для этого, он может рабоботать как сам  так и как виджет?
То есть внутри нашей онлайн записи НИЧЕГО МЕНЯТЬ НЕ НАДО!
Твоя задача подойти профессионально к том чтобы заложить новую аритетуру где онлайн заись это только часть сайта, но что бы она сейчас уже работала пока мы будем разрабатывать сайт.

И очень важно продумать как сделать так чтобы по ходу работы над сайтом ты не сломал онлайн запись  - т.к. она уже будео в проде.

Подтверди, что ты понял правила и цели, пока ответь только в чате и ничего не делай.